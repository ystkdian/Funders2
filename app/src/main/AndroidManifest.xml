<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.funders">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Funders"
        android:usesCleartextTraffic="true">
        <activity android:name=".StateActivity"></activity>
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".ui.forminvestor.InvestorFormActivity" />
        <activity android:name=".ui.forminvestor.InvestorWelcomeActivity" />
        <activity android:name=".ui.signin.SignInActivity" />
        <activity android:name=".ui.signup.FinishSignupActivity" />
        <activity android:name=".ui.formumkm.UmkmFormActivity" />
        <activity android:name=".ui.formumkm.UmkmWelcomeActivity" />
        <activity android:name=".ui.signup.ChooseRoleActivity" />
        <activity android:name=".ui.signup.SignUpActivity" />
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>